# harness-engineering

后续怎么开展工作

我建议你把后续工作流固定成这个节奏：

1. 先写任务约束。
   小任务直接改最近的 AGENTS.md 或 docs/；大任务先写一个短 spec，说明目标、边界、验收标准。
2. 再做实现。
   实现时只改和任务直接相关的目录，不顺手扩散重构。
3. 最后做规则沉淀。
   如果这次任务暴露了新的模式、目录约束、接口边界、踩坑点，就把它补进对应 AGENTS.md 或校验脚本。

这样你的仓库会慢慢从“项目”变成“会教 AI 怎么工作的项目”。
